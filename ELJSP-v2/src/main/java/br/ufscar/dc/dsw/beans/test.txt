       <jsp:useBean id="conversor" class="br.ufscar.dc.dsw.beans.ConversorServelet" scope="application" />
        <c:forEach var="i" begin="0" end="${conversor.vetor_celsius.size() - 1}">
            <tr>
                <td>${conversor.vetor_celsius[i]}</td>
                <td>${conversor.vetor_fahr[i]}</td>
                <td>${conversor.vetor_kelvin[i]}</td>
            </tr>
        </c:forEach>


    <table>
        <tr>
            <th>Celsius</th>
            <th>Fahrenheit</th>
            <th>Kelvin</th>
        </tr>
        <c:forEach var="i" begin="${minimo}" end="${maximo}" step="${incremento}">
            <c:set var="fahr" value="${1.8 * i + 32}" />
            <c:set var="kelvin" value="${i + 273.15}" />
            <tr>
                <td>${i}</td>
                <td>${fahr}</td>
                <td>${kelvin}</td>
            </tr>
        </c:forEach>
    </table>


%
    String minimoStr = request.getParameter("minimo");
    String maximoStr = request.getParameter("maximo");
    String incrementoStr = request.getParameter("incremento");

    int minimo = minimoStr != null ? Integer.parseInt(minimoStr) : -100;
    int maximo = maximoStr != null ? Integer.parseInt(maximoStr) : 100;
    int incremento = incrementoStr != null ? Integer.parseInt(incrementoStr) : 10;
    %>
    

    <p>
    ${minimo}
    </p>

